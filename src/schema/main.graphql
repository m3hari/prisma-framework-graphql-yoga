# import * from "./types.graphql"
# import * from "./inputs.graphql"

scalar DateTime
type Query {
  posts: [Post!]!
}

type Mutation {
  createPost(input: CreatePostInput!): Post!
}

schema {
  query: Query
  mutation: Mutation
}
