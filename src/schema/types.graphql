type User {
  email: String!
  id: ID!
  name: String
  posts: [Post!]!
}

type Post {
  id: ID!
  title: String!
  content: String
  published: Boolean!
  author: User
  createdAt: DateTime!
  updatedAt: DateTime!
}
